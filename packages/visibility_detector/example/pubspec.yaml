name: visibility_detector_example

publish_to: 'none'

environment:
  sdk: '3.5.4'
  flutter: '>=1.13.8'

dependencies:
  flutter:
    sdk: flutter
  visibility_detector:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter